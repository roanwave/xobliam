# Google OAuth - Download from Google Cloud Console
# Place credentials.json in project root (it's gitignored)
GOOGLE_CREDENTIALS_PATH=./credentials.json

# Optional: Custom token storage location
TOKEN_PATH=./data/credentials/token.json

# Analysis settings
ANALYSIS_DAYS=90
